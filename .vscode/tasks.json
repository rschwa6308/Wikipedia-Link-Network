{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Makefile build",
            "type": "shell",
            "command": "make",
            "group": {
                "kind": "build",
                "isDefault": true,
            },
            "problemMatcher": ["$gcc"],
            "options": {
                "cwd": "${workspaceFolder}/C++"
            }
        }
    ]
}